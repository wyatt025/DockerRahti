apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: gh05t025/lemp-frontend:1.0.2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
